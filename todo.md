# TODO

# Основные задачи

1. Переименовать репозиторий на что-то более адекватное
2. Добавить создание диплома с латехом сюда 
3. Рассказать в README про основную тему диплома и назначение пайплайна
4. Описать также, какая необходимо конфигурация + заготовить конфиги для того, чтобы воспроизвести эксперименты
5. Разбить `main.py`  на скрипта (`train` + `evaluate`) и отправить их в `src/pipeline`
6. Отправлять артефакты в `outputs/`
7. Перейти с pytorch_lightning==1.9 на pytorch_lightning>=2.0
8. Рефакторинг модулей и кода
9. Сделать единный формат для evaluate 
10. Вернуть аугментации как было (описывать Compose в конфиге), а не в src.collections.transforms/...


# Backlog 

1. Продумать коллбеки в pl и добавить их
2. Отправлять путь до ckpt и мета информацию в mlflow
3. *Посмотреть как в hydra можно заготовить различные наборы конфигов для разных экспериментов ??*
4. Подумать над вариантом, как можно удобно плоттить несколько экспериментов из mlflow, по типу ноутбука `notebooks/plot_mlflow.ipynb`